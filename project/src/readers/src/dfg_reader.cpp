#include "stdafx.h"
#include "graph.h"
#include "dfg_reader.h"

using namespace std;

Graph DFGReader::read(string path, string filename) {
	Graph graph = Graph(10);
	return graph;
}

//READ TERMINAL LINES SEPERATELY AND IGNORE WEIGHTS