#include "stdafx.h"
#include "naive_galois.h"
#include "matroid.h"
#include "gauss.h"

using namespace std;

int main(int argc, char* argv[]) {
	
	//Matroid matroid(elements, matroidRank);
	
	//Galois* galois = new 
	
	//vector<int> independentElements;
	//independentElements.push_back(2);
	//independentElements.push_back(5);
	//independentElements.push_back(8);
	//independentElements.push_back(6);
	
	//bool independent = Gauss::isIndependentSet(independentElements, rank, matroid, galois);
	
	//if (!independent) {
	//	return 1;
	//}
	
	
	
	//bool independent = Gauss::isIndependentSet(NULL, 0, matroid, NULL);
	//if (!independent) { //TODO: actually check that the matroid is correct
	//	return 1;
	//}
	return 0;
}